[project]
name = '-Autonomous-Portfolio-Rebalancer'
description = ''
authors = []
telemetry = true
cache_dir = '.\.cache'
requirements = []

[contracts.rebalance-fee-engine]
path = 'contracts/rebalance-fee-engine.clar'
clarity_version = 3
epoch = 'latest'

[contracts.keeper-registry]
path = 'contracts/keeper-registry.clar'
clarity_version = 3
epoch = 'latest'

[contracts.auto-portfolio-rebalancer]
path = 'contracts/auto-portfolio-rebalancer.clar'
clarity_version = 3
epoch = 'latest'

[contracts.strategy-registry]
path = 'contracts/strategy-registry.clar'
clarity_version = 3
epoch = 'latest'

[contracts.performance-tracker]
path = 'contracts/performance-tracker.clar'
clarity_version = 3
epoch = 'latest'

[contracts.portfolio-lock]
path = 'contracts/portfolio-lock.clar'
clarity_version = 3
epoch = 'latest'

[repl.analysis]
passes = ['check_checker']

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false

[repl.remote_data]
enabled = false
api_url = 'https://api.hiro.so'
